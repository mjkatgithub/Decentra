export default defineAppConfig({
  ui: {
    input: {
      slots: {
        root: 'relative inline-flex w-full items-center'
      }
    },
    formField: {
      slots: {
        container: 'mt-1 w-full'
      }
    }
  }
})
